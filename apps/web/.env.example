# Atlas Frontend Environment Configuration
# Copy to .env.local for development

# API base URL — the path prefix for all API requests.
# In development with Vite proxy, use the default /api/v1.
# In production, set to the full backend URL.
VITE_API_BASE_URL=/api/v1
